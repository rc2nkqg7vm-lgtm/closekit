// If `geist` package is unavailable, replace app/layout.tsx font imports with:
//
//   import { Inter } from "next/font/google";
//   const inter = Inter({ subsets: ["latin"], variable: "--font-geist-sans" });
//
// and update <html> to:
//   <html lang="en" className={inter.variable}>
//
// Or install the Geist package:
//   npm install geist
//
// This file is a reference note only — delete it before deploying.

export {};
